ADMIN_TOKEN=ADMIN_TOKEN123
# Public base URL of this service. Hosts the per-OA LINE webhooks
# ({SERVICE_BASE_URL}/webhook/{token}) — must be publicly reachable.
SERVICE_BASE_URL=http://localhost:5002
DATABASE_URL=sqlite:///./line_tms.db